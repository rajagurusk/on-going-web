specVersion: "v1"
serviceId: "on-going-web"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "on-going-web-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
